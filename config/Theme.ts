const colors = {
  primary: '#ff6343', // Color for buttons or links
  secondary: '#ffe6e1',
  bg: '#fff', // Background color
  white: '#fff',
  grey: {
    dark: '#212529',
    default: 'rgba(0, 0, 0, 0.7)',
    light: '#6c757d',
    ultraLight: '#f8f9fa',
  },
};

const transitions = {
  normal: '0.32s',
};

const fontSize = {
  small: '.75rem',
  big: '2.5rem',
};

export default {
  colors,
  transitions,
  fontSize,
};
